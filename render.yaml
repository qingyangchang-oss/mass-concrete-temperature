# Render deployment configuration
services:
  - type: web
    name: mass-concrete-temp
    env: docker
    plan: free
    healthCheckPath: /
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
